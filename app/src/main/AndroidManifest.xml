<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomallocation">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Teacher.ReallocateRoom"></activity>
        <activity android:name=".SelectType" />
        <activity android:name=".Student.StudentList" />
        <activity android:name=".Student.StudentMain" />
        <activity android:name=".Teacher.ExtraClass" />
        <activity android:name=".Teacher.TeacherList" />
        <activity android:name=".Teacher.TeacherMain" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>